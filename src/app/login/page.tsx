"use client";
import Link from "next/link";import {useState} from "react";import {api,saveSession} from "../../lib/api";import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();const signIn=async(demo=false)=>{try{setError("");const data=await api<{token:string;user:{name:string;email:string}}>(demo?'/auth/demo':'/auth/login',{method:'POST',body:JSON.stringify(demo?{}:{email,password})});saveSession(data);router.push('/dashboard')}catch(e){setError(e instanceof Error?e.message:'Sign in failed')}};return <main className="auth"><form onSubmit={e=>{e.preventDefault();signIn()}}><Link className="brand" href="/"><span>◆</span> DevSprint <i>AI</i></Link><div><div className="eyebrow">Welcome back</div><h1>Sign in to your workspace.</h1><p>Pick up exactly where your last sprint left off.</p></div><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><label>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<p className="form-error">{error}</p>}<button className="button primary wide">Sign in</button><button type="button" className="social" onClick={()=>setError('Google OAuth needs Google credentials and callback setup on the server.')}>G&nbsp;&nbsp; Continue with Google</button><button type="button" className="demo" onClick={()=>signIn(true)}>Use demo account</button><small>New to DevSprint? <Link href="/register">Create an account</Link></small></form></main>}
